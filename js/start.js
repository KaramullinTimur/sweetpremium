const start = document.querySelector('.start');
const button_wholesale = document.querySelector('.start-button-wholesale');

button_wholesale.addEventListener('click', () => {
    start.classList.remove('active');
})
